# Project Brain Knowledge Base

Markdown files in this directory are the canonical, human-reviewable Project Brain source.
The `.brain/.index` directory may contain derived, reproducible indexes in a future slice; it
must not become an authoring source. Generated Items and revisions must never silently become
approved—critical project memory remains subject to its authority and approval policy.

Run `brain validate .brain` to validate this knowledge base.
